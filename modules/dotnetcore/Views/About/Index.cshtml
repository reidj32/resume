@model ResumeModel

@section navbar {
   @await Html.PartialAsync("_Navbar", Model)
}

<h1>
   <i class="fas fa-info-circle"></i> @Model.About.Title
</h1>

@foreach (var paragraph in Model.About.Paragraphs)
{
   <p>@paragraph</p>
}
